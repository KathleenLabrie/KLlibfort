	SUBROUTINE GET_SEED0 ()
	INTEGER*4 ISEED, IARRAY(3)
	EXTERNAL ITIME
	common/random/iseed
	DATA NFLUSH/1000/
	CALL ITIME (IARRAY)
	SECNDS=3600.*IARRAY(1)+60.*IARRAY(2)+IARRAY(3)
	ISEED=nint(1.E7+SECNDS)*2+1
	Y=RAN0(ISEED)
	DO 1 I=1,NFLUSH
1	Y=RAN0(0)
	RETURN
	END
